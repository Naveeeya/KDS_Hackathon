# LLM API Configuration
# Option 1: Google Gemini (Recommended - generous free tier)
# Get your free API key from: https://aistudio.google.com/app/apikey
# Free tier: 60 requests/minute
GEMINI_API_KEY=your_gemini_api_key_here

# Option 2: OpenAI (Fallback)
# Get your API key from: https://platform.openai.com/api-keys
# Free tier includes $5 credit for testing
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Note: System will auto-select available provider
# Priority: Gemini (best free tier) → OpenAI → Constraint-only fallback
